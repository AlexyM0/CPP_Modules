#pragma once

#include "iostream"

class Span
{
    public:
        Span();
        Span(unsigned int);
        ~Span();
        addNumber();
        shortestSpan();
        longestSpan();
    
    private:
        unsigned int _size;


};